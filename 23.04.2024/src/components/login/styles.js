import { styled } from "@mui/material";

export const SignFromStyle = styled("div")({
  ".style": {
    backgroundColor: "red",
  },
  ".sign": {
    display: "flex",
    justifyContent: "center",
  },
  ".google": {
    display: "flex",
    justifyContent: "center",
    height: "50px",
    width: "280px",
  },
});
